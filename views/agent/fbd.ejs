<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>layui</title>
    <meta name="renderer" content="webkit">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <link rel="stylesheet" href="/public/lib/layui-v2.6.3/css/layui.css" media="all">
    <link rel="stylesheet" href="/public/css/public.css" media="all">
</head>
<body>
<div class="layuimini-container">
    <div class="layuimini-main">

        <fieldset class="table-search-fieldset">
            <legend>搜索信息</legend>
            <div style="margin: 10px 10px 10px 10px">
                <form class="layui-form layui-form-pane" action="">
                    <div class="layui-form-item">
                        <div class="layui-inline">
                            <label class="layui-form-label">联赛名称</label>
                            <div class="layui-input-inline">
                                <input type="text" name="league" autocomplete="off" class="layui-input">
                            </div>
                        </div>
                    </div>
                   
                    <div class="layui-form-item"> 
                        <div class="layui-inline">
                            <label class="layui-form-label">主队</label>
                            <div class="layui-input-inline">
                                <input type="text" name="homeTeam" autocomplete="off" class="layui-input">
                            </div>
                        
                            <div class="layui-form-mid">vs</div>
                       
                            <label class="layui-form-label">客队</label>
                            <div class="layui-input-inline">
                                <input type="text" name="awayTeam" autocomplete="off" class="layui-input">
                            </div>
                        </div>
                    </div>
                   
                    <div class="layui-form-item"> 
                        <div class="layui-inline" id="dvregtime" >
                            <label class="layui-form-label">开赛时间</label>
                            <div class="layui-input-inline">
                                <input type="text" name="beginTime" autocomplete="off" id="txtbegintime" class="layui-input" placeholder="开始日期"  >
                            </div>
                            <div class="layui-form-mid">-</div>
                            <div class="layui-input-inline">
                                <input type="text" name="endTime" autocomplete="off" id="txtendtime" class="layui-input" placeholder="结束日期"  >
                            </div>
                        </div>
                        <div class="layui-inline">
                            <button id="btnSearch" type="submit" class="layui-btn layui-btn-primary"  lay-submit lay-filter="data-search-btn"><i class="layui-icon"></i> 搜 索</button>
                        </div>
                    </div>

                </form>
                
            </div>
        </fieldset>

        <script type="text/html" id="toolbarDemo">
            <!--div class="layui-btn-container">
                <button class="layui-btn layui-btn-normal layui-btn-sm data-add-btn" lay-event="add"> 添加 </button>
                <button class="layui-btn layui-btn-sm layui-btn-danger data-delete-btn" lay-event="delete"> 删除 </button>
            </div-->
        </script>

        <table class="layui-hide" id="currentTableId" lay-filter="currentTableFilter"></table>

        <script type="text/html" id="currentTableBar">
            <a class="layui-btn layui-btn-normal layui-btn-xs data-count-edit" lay-event="edit">反波赔率</a>
            <!--a class="layui-btn layui-btn-xs layui-btn-danger data-count-delete" lay-event="delete">删除</a-->
        </script>

    </div>
</div>
<script src="/public/lib/layui-v2.6.3/layui.js" charset="utf-8"></script>
<script src="/public/lib/laydate/laydate.js" charset="utf-8"></script>
<script>
    layui.use(['form', 'table','jquery','laydate'], function () {
        var $ = layui.jquery,
            form = layui.form,
            laydate = layui.laydate,
            table = layui.table;

        laydate.render({
            elem: '#dvregtime'
            //设置开始日期、日期日期的 input 选择器
            //数组格式为 5.3.0 开始新增，之前版本直接配置 true 或任意分割字符即可
            ,range:['#txtbegintime','#txtendtime']
            ,type: 'datetime'
        });

        
        table.render({
            elem: '#currentTableId',
            type:'post',
            url: '/admin/fbd_data',
            toolbar: '#toolbarDemo',
            defaultToolbar: ['filter', 'exports', 'print', {
                title: '提示',
                layEvent: 'LAYTABLE_TIPS',
                icon: 'layui-icon-tips'
            }],
            cols: [[
                /*{type: "checkbox", width: 50},*/
                {field: 'id', width: 100, title: 'ID', sort: true},
                {field: 'league', width: 160, title: '联赛'}, 
                {field: 'hometeam', width: 160, title: '主队', sort: true},
                {field: 'awayteam', width: 160, title: '客队'},
                {field: 'matchTime', width: 160,title: '开赛时间', minWidth: 150},
                {field: 'balltype', width: 80, title: '类型', sort: true},
                {field: 'created', width: 200, title: '创建时间', sort: true},
                {field: 'updated', width: 200, title: '更新时间', sort: true},
               
                {title: '操作',   toolbar: '#currentTableBar', align: "center" ,fixed:'right'}
            ]],
            limits: [10, 15, 20, 25, 50, 100],
            limit: 15,
            page: true,
            skin: 'line'
        });

        // 监听搜索操作
        form.on('submit(data-search-btn)', function (data) {
            var result = JSON.stringify(data.field);
            
           /* layer.alert(result, {
                title: '最终的搜索信息'
            });*/

            //执行搜索重载
            table.reload('currentTableId', {
                page: {
                    curr: 1
                }
                , where: {
                    searchParams: result
                }
            }, 'data');

            return false;
        });

        /**
         * toolbar监听事件
         */
        table.on('toolbar(currentTableFilter)', function (obj) {
            if (obj.event === 'add') {  // 监听添加操作
                var index = layer.open({
                    title: '添加用户',
                    type: 2,
                    shade: 0.2,
                    maxmin:true,
                    shadeClose: true,
                    area: ['100%', '100%'],
                    content: '../page/table/add.html',
                });
                $(window).on("resize", function () {
                    layer.full(index);
                });
            } else if (obj.event === 'delete') {  // 监听删除操作
                var checkStatus = table.checkStatus('currentTableId')
                    , data = checkStatus.data;
                layer.alert(JSON.stringify(data));
            }
        });

        //监听表格复选框选择
        table.on('checkbox(currentTableFilter)', function (obj) {
            console.log(obj)
        });

        table.on('tool(currentTableFilter)', function (obj) {
            var data = obj.data;
            if(obj.event === 'itemUpdate'){
                console.log('itemUpdate');
            }
            else if (obj.event === 'edit') {
                var html = '<table style="width:100%;padding:10px;text-align:center">';
                    html += '<tr class="layui-form-item">\
                        <td>比分</td>\
                        <td>正波赔率</td>\
                        <td>反波赔率</td>\
                        <td>赔率折扣</td>\
                    </tr>'
                for(let i in data.fbd_data){
                    html += '<tr class="layui-form-item">\
                        <td>'+(data.fbd_data[i].cutline[0])+'</td>\
                        <td>'+(data.fbd_data[i].cutline[4])+'</td>\
                        <td>'+(data.fbd_data[i].cutline[1])+'</td>\
                        <td align="center"><input id="'+data.fbdid+'" type="text" name="fbdratedz" style="width:80px" onchange="changeDz(this,\''+data.fbd_data[i].cutline[0]+'\')"  autocomplete="off" class="layui-input" value="'+(data.fbd_data[i].cutline[2])+'" lay-event="itemUpdate"></td>\
                    </tr>'
                }
                html += '</table>'
                //layer.alert(html)
                var index = layer.open({
                    title: '查看反波赔率',
                    type: 1,
                    shade: 0.2,
                    maxmin:true,
                    shadeClose: true,
                    area: ['40%', '70%'],
                    //content: '../page/table/edit.html',
                    content:html,
                });
                $(window).on("resize", function () {
                    layer.full(index);
                });
                return false;
            } else if (obj.event === 'delete') {
                layer.confirm('真的删除行么', function (index) {
                    obj.del();
                    layer.close(index);
                });
            }
        });

        

    });


    function changeDz(obj,cutline){
            console.log(obj);
            console.log(obj.id)
            console.log(obj.value)

            var $ = layui.jquery;
            $.ajax({
                url:'/admin/fbd_data/dz',
                type:'post',
                data:{
                    fbdid:obj.id,
                    cutline:cutline,
                    dz:obj.value
                },
                dataType:'json',
                success:function(res){
                    console.log(res)
                    $('#btnSearch').click();
                    //window.location.reload();
                }
            })
        }
</script>

</body>
</html>