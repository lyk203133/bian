<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>layui</title>
    <meta name="renderer" content="webkit">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <link rel="stylesheet" href="/public/lib/layui-v2.6.3/css/layui.css" media="all">
    <link rel="stylesheet" href="/public/css/public.css" media="all">
</head>

<body>
    <div class="layuimini-container">
        <div class="layuimini-main">

            <fieldset class="table-search-fieldset">
                <legend>搜索信息</legend>
                <div style="margin: 10px 10px 10px 10px">
                    <form class="layui-form layui-form-pane" action="">
                        <div class="layui-form-item">
                            <div class="layui-inline">
                                <label class="layui-form-label">操作人</label>
                                <div class="layui-input-inline">
                                    <input type="text" name="op" autocomplete="off" class="layui-input">
                                </div>
                            </div>

                            <div class="layui-inline">
                                <label class="layui-form-label">會員帳號</label>
                                <div class="layui-input-inline">
                                    <input type="text" name="keyword" autocomplete="off" class="layui-input">
                                </div>
                            </div>


                        </div>
                        <div class="layui-form-item">
                            <div class="layui-inline" id="dvregtime">
                                <label class="layui-form-label">時間</label>
                                <div class="layui-input-inline">
                                    <input type="text" name="begintime" autocomplete="off" id="txtbegintime"
                                        class="layui-input" placeholder="開始日期">
                                </div>
                                <div class="layui-form-mid">-</div>
                                <div class="layui-input-inline">
                                    <input type="text" name="endtime" autocomplete="off" id="txtendtime"
                                        class="layui-input" placeholder="結束日期">
                                </div>
                            </div>
                            <div class="layui-inline">
                                <input type="hidden" name="status" value="1" />
                                <button type="submit" class="layui-btn layui-btn-primary" lay-submit
                                    lay-filter="data-search-btn"><i class="layui-icon"></i> 搜 索</button>
                            </div>
                        </div>

                    </form>

                </div>
            </fieldset>

            <script type="text/html" id="toolbarDemo">
            <!--div class="layui-btn-container">
                <button class="layui-btn layui-btn-normal layui-btn-sm data-add-btn" lay-event="add"> 添加 </button>
                <button class="layui-btn layui-btn-sm layui-btn-danger data-delete-btn" lay-event="delete"> 刪除 </button>
            </div-->
        </script>

            <table class="layui-hide" id="currentTableId" lay-filter="currentTableFilter"></table>

            <script type="text/html" id="currentTableBar">
                <a class="layui-btn layui-btn-normal layui-btn-xs data-count-info" lay-event="info">查看</a>
            </script>

            <script type="text/html" id="switchTableBar">
            <input type="checkbox" name="close" value="{{d.id}}" lay-skin="switch" lay-filter="status-enable"  lay-text="正常|鎖定"  {{ d.status == 1 ? 'checked' : '' }}>
        </script>

        </div>
    </div>
    <script src="/public/lib/layui-v2.6.3/layui.js" charset="utf-8"></script>
    <script src="/public/lib/laydate/laydate.js" charset="utf-8"></script>
    <script>
        layui.use(['form', 'table', 'laydate'], function () {
            var $ = layui.jquery,
                form = layui.form,
                laydate = layui.laydate,
                table = layui.table;

            laydate.render({
                elem: '#dvregtime'
                //設置開始日期、日期日期的 input 選擇器
                //數組格式爲 5.3.0 開始新增，之前版本直接配置 true 或任意分割字符即可
                , range: ['#txtbegintime', '#txtendtime']
                , type: 'datetime'
            });


            table.render({
                elem: '#currentTableId',
                url: '/admin/market/list_data?status=1',

                toolbar: '#toolbarDemo',
                defaultToolbar: ['filter', 'exports', 'print', {
                    title: '提示',
                    layEvent: 'LAYTABLE_TIPS',
                    icon: 'layui-icon-tips'
                }],
                cols: [[
                    //{ type: "checkbox" },
                    //{ field: 'id', title: 'ID', sort: true },
                    { field: 'taskTime', title: '時間', align: "center" },
                    { field: 'userName', title: '會員賬號', sort: true },
                    { field: 'typeName', title: '申請類型' },
                    { field: 'statusName', title: '結果' },
                    { field: 'comment', title: '備注' },
                    //{ field: 'op', title: '操作客服賬號' },

                    { title: '操作', toolbar: '#currentTableBar', align: "center" }
                ]],
                limits: [10, 15, 20, 25, 50, 100],
                limit: 15,
                page: true,
                skin: 'line'
            });

            //監聽指定開關
            form.on('switch(status-enable)', function (data) {
                /*layer.msg('開關checked：' + (this.checked ? 'true' : 'false'), {
                    offset: '6px'
                });
                layer.tips('溫馨提示：請注意開關狀態的文字可以隨意定義，而不僅僅是ON|OFF', data.othis)
    */

                var id = this.value;
                var status = this.checked ? 1 : 0;
                layer.confirm('確定操作？', { icon: 3, title: '提示' }, function (index) {
                    layer.close(index);
                    let loading = layer.load();
                    $.ajax({
                        url: '/admin/discount/status/' + id + '/' + status,
                        dataType: 'json',
                        type: 'get',
                        success: function (result) {
                            layer.close(loading);
                            if (result.code == 0) {
                                layer.msg(result.message, { icon: 1, time: 1000 }, function () {
                                    //table.reload('role-table');
                                });
                            } else {
                                layer.msg(result.message, { icon: 2, time: 1000 });
                            }
                        }
                    })
                });
            });

            // 監聽搜索操作
            form.on('submit(data-search-btn)', function (data) {
                var result = JSON.stringify(data.field);

                /* layer.alert(result, {
                     title: '最終的搜索信息'
                 });*/

                //執行搜索重載
                table.reload('currentTableId', {
                    page: {
                        curr: 1
                    }
                    , where: {
                        searchParams: result
                    }
                }, 'data');

                return false;
            });

            /**
             * toolbar監聽事件
             */
            table.on('toolbar(currentTableFilter)', function (obj) {
                if (obj.event === 'add') {  // 監聽添加操作
                    var index = layer.open({
                        title: '添加用戶',
                        type: 2,
                        shade: 0.2,
                        maxmin: true,
                        shadeClose: true,
                        area: ['80%', '100%'],
                        content: '/admin/discount/add',
                    });
                    $(window).on("resize", function () {
                        layer.full(index);
                    });
                } else if (obj.event === 'delete') {  // 監聽刪除操作
                    var checkStatus = table.checkStatus('currentTableId')
                        , data = checkStatus.data;
                    layer.alert(JSON.stringify(data));

                    layer.confirm('確定操作？', { icon: 3, title: '提示' }, function (index) {
                        layer.close(index);
                        let loading = layer.load();
                        var params = [];
                        for (let i in data) {
                            params.push(data[i].id)
                        }
                        $.ajax({
                            url: '/admin/discount/delete',

                            type: 'post',
                            dataType: 'json',
                            data: { ids: params },
                            success: function (result) {
                                layer.close(loading);
                                if (result.code == 0) {
                                    layer.msg(result.message, { icon: 1, time: 1000 }, function () {
                                        table.reload('currentTableId');
                                    });
                                } else {
                                    layer.msg(result.message, { icon: 2, time: 1000 });
                                }
                            }
                        })
                    });
                }
            });

            //監聽表格複選框選擇
            table.on('checkbox(currentTableFilter)', function (obj) {
                console.log(obj)
            });

            table.on('tool(currentTableFilter)', function (obj) {
                var data = obj.data;
                if (obj.event === 'fail') {
                    var index = layer.open({
                        title: '編輯用戶',
                        type: 2,
                        shade: 0.2,
                        maxmin: true,
                        shadeClose: true,
                        area: ['80%', '100%'],
                        content: '/admin/market/' + data.id,
                    });
                    $(window).on("resize", function () {
                        layer.full(index);
                    });
                    return false;
                } else if (obj.event === 'info') {

                    console.log(data)
                    var index = layer.open({
                        title: '查看',
                        type: 2,
                        shade: 0.2,
                        maxmin: true,
                        shadeClose: true,
                        area: ['60%', '60%'],
                        content: '/admin/market/info/' + data.id,
                        //content: '<p class="task-title">任務類型：' + data.typeName + '</p><p class="task-title">會員：' + data.userName + '</p><p class="task-title">圖片：<img src="' + data.info.photo + '" style="width:150px"/></p>',
                    });
                    $(window).on("resize", function () {
                        layer.full(index);
                    });
                    return false;
                } else if (obj.event === 'success') {
                    console.log(data);
                    layer.confirm('確認成功麽', function (index) {
                        obj.del();
                        layer.close(index);

                        data.status = 2;
                        data.comment = '成功';
                        $.ajax({
                            url: '/admin/market/save',
                            type: 'post',
                            data: data,
                            success: function (res) {
                                console.log(res)

                                if (res.code == 0) {
                                    layer.msg(res.message, { icon: 1, time: 1000 }, function () {
                                        table.reload('currentTableId');
                                    });
                                } else {
                                    layer.msg(res.message, { icon: 2, time: 1000 });
                                }
                            }
                        })
                    });
                }
            });
        });



    </script>

</body>

</html>